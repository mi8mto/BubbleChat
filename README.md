# React + Vite

BubbleChat – a modern web chat with bubble animations, built with React and WebSocket for real-time messaging between tabs and users. Supports separate usernames for each tab, stores the username in sessionStorage, includes notifications for user connections/disconnections, and features smooth auto-scrolling.

deploy -
